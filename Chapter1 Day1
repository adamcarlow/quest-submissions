"Explain what the Blockchain is in your own words."  - A blockchain is a decentralized, public database which stores new data into blocks which are linked together and form a chain. Transactions are immutable and all users with access to the network can interact with them.

 
